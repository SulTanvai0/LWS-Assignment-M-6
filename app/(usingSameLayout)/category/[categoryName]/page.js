import CategoryContainer from "@/components/CategoryContainer";

export const metadata = {
  title: "category Page",
};

const page = () => {
  return (
    <div>
      <CategoryContainer />
    </div>
  );
};

export default page;
